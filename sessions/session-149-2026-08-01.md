# Session 149 — 2026-08-01

**Model:** Mid-Range — Sonnet 4.6 (Morwen desktop surface)
**Duration:** ~3 hours

## Deliverables

### 1. Registry Cleanup Run-Once Guard — TWO BUGS FIXED + DEPLOYED
FRED's delivery (aa8d219) had two bugs preventing full completion:
- **Bug 1:** Step 7 (delete junk components) failed on 6 components because `randall_alerts` (FK `component_id`) was not in the `_CHILD_TABLES_COMPONENT_ID` cascade list. Fixed by adding `randall_alerts` to the list.
- **Bug 2:** Completion marker INSERT used raw SQL bypassing SQLAlchemy defaults — `config_version` column is NOT NULL with no DB-side default. INSERT had 3 values for 4 columns. Fixed by adding `config_version = 1` to the INSERT.
- Both fixes applied directly on waldo-vm, SCP'd to Windows, pushed to origin as commit `27866c5`.
- Deployed. All 6 junk components confirmed GONE. Marker written — guard skips on all future restarts.
- `registry_cleanup` board item: **CLOSED**.

### 2. Gateway Dockerfile CMD Fix — PUSHED
- `python -m uvicorn` fix (from session 148 local sed) committed and pushed from CT 113 directly.
- Commit `10409a6` on waldo-mcp-gateway main. Anyone cloning gets a working build now.

### 3. Gateway Registered as GovernedComponent in WALDO
- **ID:** 5b647c1b-9223-4513-a9ae-9fcef5d8a8fc
- **Type:** Studio Project → should be corrected to tool/mcp_gateway
- **Risk Tier:** T3 — Standard
- **Governance Posture:** Operational (corrected from Innovation)
- **Repo:** MissingLinkThag/waldo-mcp-gateway (typo in initial entry: `tnag` → `thag`, flagged)
- **Runtime:** Docker/Container
- **Integration Protocol:** REST API
- **Version:** 1.0.0
- Baseline created with full deployment state description.

### 4. SQLAlchemy Query.get() Deprecation — FRED DELIVERED + DEPLOYED
- Spec `fred_spec_query_get_deprecation.json` pushed (commit `52f8755`).
- FRED delivered `d5cd300`: 29 files, 124 additions / 122 deletions. Pure mechanical `Model.query.get(id)` → `db.session.get(Model, id)`.
- Reviewed: PASS. Two new `from app import db` imports added correctly (gateway.py, github_webhook.py).
- Deployed to waldo-vm: 701 passed, 66 warnings (down from 805). Remaining warnings are a different type.

### 5. Alignment Review Spec REVISED (v1.0 → v1.1) + DISPATCHED
- Part C reworked: removed fixed penalty constants (-200/-100), replaced with grade multiplier system (0.4/0.7/1.0) that feeds through the existing correctness axis formula.
- v1.1 pushed to repo replacing v1.0. FRED dispatched, building.
- Key design move: alignment_failure_mode adjusts the INPUT grade BEFORE the formula runs, not penalties AFTER.

## Repo State
- **waldo-cis2:** HEAD `b83fa0f` (deployed to waldo-vm, 701 tests green)
- **waldo-mcp-gateway:** HEAD `10409a6` (CMD fix pushed, deployed on CT 113)
- **helm:** unchanged

## Open Threads
1. MCP_ADMIN_KEY from Andrew — still waiting
2. FRED alignment review delivery — building
3. Layer 4 alignment review spec — in repo but FRED hasn't built it
4. Andrew's sidecar header changes — his side
5. Template sync waldo-template ← waldo-cis2 — Marcus timing call
6. Gateway component type correction (studio_project → tool/mcp_gateway)
7. Remaining 66 test warnings — different root cause from Query.get(), low priority