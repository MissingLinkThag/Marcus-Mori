# Session 148 — 2026-07-31

**Date:** 2026-07-31
**Model:** Mid-Range — Sonnet 4.6
**Duration:** ~5h 18m

## Summary

Major infrastructure and architecture session. Three milestones: Andrew's gateway design accepted, FRED built the auth layer, and the gateway CT is deployed and live on the Lab.

### 1. Andrew's Gateway Design v2 — ACCEPTED with 3 refinements

Andrew responded to Marcus's v1 MCP Gateway proposal with a full redesign:
- **Discovery-time authorization** replaces per-call evaluate (one WALDO call per session, not per tool call)
- **Three-mode auth** (waldo/kindra/static) — different price points, same gateway binary
- **Clean Kindra product boundaries** — Kindra works without WALDO, WALDO enriches when connected
- **Option B identity** — X-User-ID (stable UUID) + Bearer token (rotating, per-call)

Marcus/Mori pushed back on 3 items, all accepted by Andrew:
1. **v2 trigger for content-sensitive auth** — named trigger: "first customer with multi-team data segregation"
2. **TTL 300→900 + stale_grant_policy** — deny (default) or extend (serve stale grant with warning)
3. **Docker network isolation → IP allowlist for multi-host** — Docker works single-host, IP check in MCP Server Template for separated topology

### 2. WALDO v2 Redesign — Ground Truth Status from Repo

Full repo audit confirmed:
- **Layer 0 (Foundation):** HOLDING — 701 tests green, data recovered
- **Layer 1 (Structural):** CODE ON MAIN — SkillDefinition, SkillAuthorization, AgentRelationship, RANDALL (8 rules seeded), all tables exist
- **Layer 2 (Telemetry + Execution):** CODE ON MAIN — TelemetrySession, SkillExecution, SessionCostSummary, SkillPerformanceMetric, services, routes, tests. Tables exist but empty (waiting for gateway to feed them)
- **Layer 3 (Gateway):** DESIGNED AND NOW BUILT — spec locked, FRED delivered, deployed
- **Layer 4 (Intelligence):** PARTIALLY SPECCED — alignment_review spec in repo, RANDALL operational
- **Layer 5 (Experience):** Panorama rename done, rest not started

Key insight: Layer 3 was the blocking decision. Andrew's response unblocked it. WALDO needs zero code changes for the gateway — existing APIs are sufficient.

### 3. Gateway FRED Spec Written + Delivered + Reviewed CLEAN

Spec `fred_spec_gateway_auth_v1.json` pushed to MissingLinkThag/waldo-mcp-gateway. FRED delivered commit `86ae41a`:
- `gateway/auth.py` — identity extraction ✓
- `gateway/authorization.py` — three-backend auth cache, 900s TTL, stale_grant_policy ✓
- `gateway/telemetry.py` — tool call recording + buffered flush ✓
- `main.py` modified — filtered /tools, proxy guard, webhook, cache stats ✓
- `proxy.py` modified — body bytes param to avoid double-read ✓
- `config.py` modified — AuthConfig + TelemetryConfig ✓
- CIS2 profile Dockerfile (multi-stage, hash-pinned, non-root) ✓
- 20 tests ✓
- Body-read-once critical path correctly handled ✓

### 4. CT 113 `waldo-gateway` Deployed on Proxmox Lab

- CT 113 at 192.168.60.40:8000
- Separated topology: gateway on .40, MCP servers on 106 (.20)
- All 6 MCP servers health-checked and healthy
- Authorization disabled (Phase 1 — pure passthrough)
- Tool discovery returns 403 — needs MCP_ADMIN_KEY from Andrew (message sent)
- Dockerfile CMD fix applied (uvicorn → python -m uvicorn)

Migration path ready:
- Phase 1 (now): auth disabled, pure passthrough
- Phase 2: static auth from config
- Phase 3: kindra auth
- Phase 4: waldo auth (full governance)

### 5. FRED Registry Cleanup Run-Once Guard — DELIVERED, reviewed clean

Commit `aa8d219` on waldo-cis2. Run-once guard using system_config table. Destructive cleanup steps (1-7) now execute once and skip on all subsequent boots. Zero changes to schema patches. Not yet deployed to waldo-vm.

### 6. Lab Topology Update

| CT/VM | Name | IP | Purpose |
|-------|------|----|---------|
| 101 | irc-lab-db | .11 | PostgreSQL |
| 102 | kai-lab-waldo | .12 | WALDO instance |
| 103 | kai-lab-vilkd | ? | Vilk |
| 106 | morwen-mcp | .20 | Andrew's MCP servers |
| 113 | waldo-gateway | .40 | MCP Gateway (NEW) |

## Open Threads

1. **MCP_ADMIN_KEY from Andrew** — gateway tool discovery blocked without it
2. **Deploy registry-cleanup-runonce to waldo-vm** — reviewed clean, not yet deployed
3. **Commit Dockerfile CMD fix** to waldo-mcp-gateway repo (local-only change)
4. **Register gateway as GovernedComponent in WALDO** — needs component ID + API key for Phase 4
5. **Layer 4 alignment review** — check if fred_spec_alignment_review.json was delivered
6. **SQLAlchemy Query.get() warnings** — 805 warnings, mechanical FRED work, session 137 board item
7. **Sidecar changes** — Andrew's side: wire X-Agent-ID/X-Agent-Key/X-User-ID headers

## Repo State

- **waldo-cis2:** HEAD `aa8d219` (registry-cleanup-runonce), NOT deployed to waldo-vm
- **waldo-mcp-gateway:** HEAD `86ae41a` (FRED auth delivery), DEPLOYED to CT 113 at .40:8000
- **helm:** unchanged this session

## Model Routing Log

S148 | 2026-07-31 | Andrew v2 review + gateway spec + FRED review + CT deploy + registry-cleanup review | Mid-Range | Sonnet 4.6 | Y | 0 | --